import { Box } from '@mui/material';

export default function SpectrumSettings() {
  return (
    <Box
      sx={{
        height: '100%',
        background: '#eee',
      }}
    >
      Spectrum
    </Box>
  );
}
